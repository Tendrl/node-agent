<Plugin "python">
    ModulePath "/usr/lib64/collectd/gluster/low_weight"

    Import "tendrl_glusterfs_peer_network_throughput"

    <Module "tendrl_glusterfs_peer_network_throughput">
        integration_id "{{integration_id}}"
        graphite_host "{{ graphite_host }}"
        graphite_port "{{ graphite_port }}"
        peer_name "{{ hostname }}"
    </Module>
</Plugin>
